version: "3"
services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
  
  app:
    build: .
    environment:
      REDIS_ADDR: "redis:6379"  # Use the container name as the address
    depends_on:
      - redis
